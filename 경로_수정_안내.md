# .env 파일 경로 수정 안내

## ✅ 찾은 정보

**oci.dll 파일 위치**:
```
C:\oraclexe\instantclient-basic-windows.x64-23.26.0.0.0\instantclient_23_0\oci.dll
```

**올바른 경로**: `oci.dll`이 있는 폴더까지의 경로
```
C:\oraclexe\instantclient-basic-windows.x64-23.26.0.0.0\instantclient_23_0
```

---

## 🔧 .env 파일 수정하기

### 1단계: .env 파일 열기

```powershell
notepad .env
```

### 2단계: 경로 수정

기존 경로를 찾아서 다음과 같이 **수정**하세요:

**기존** (잘못된 경로):
```
ORACLE_INSTANT_CLIENT_PATH=C:\oraclexe\instantclient-basic-windows.x64-23.26.0.0.0
```

**수정 후** (올바른 경로):
```
ORACLE_INSTANT_CLIENT_PATH=C:\oraclexe\instantclient-basic-windows.x64-23.26.0.0.0\instantclient_23_0
```

또는 더 간단하게:
```
ORACLE_INSTANT_CLIENT_PATH=C:\oraclexe\instantclient-basic-windows.x64-23.26.0.0.0\instantclient_23_0
```

### 3단계: 저장

**Ctrl + S** 키를 누르거나 **파일 → 저장**

---

## 🧪 다시 테스트

경로를 수정한 후:

```powershell
python test_oracle_thick.py
```

이제 성공해야 합니다!

---

## 💡 한 번에 수정하는 방법 (PowerShell)

PowerShell에서 한 번에 수정:

```powershell
(Get-Content .env) -replace 'ORACLE_INSTANT_CLIENT_PATH=.*', 'ORACLE_INSTANT_CLIENT_PATH=C:\oraclexe\instantclient-basic-windows.x64-23.26.0.0.0\instantclient_23_0' | Set-Content .env
```

이 명령이 자동으로 경로를 수정해줍니다!



