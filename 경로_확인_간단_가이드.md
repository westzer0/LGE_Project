# Oracle Instant Client 경로 빠른 확인

## 🎯 간단 확인 방법

### 방법 1: 배치 파일 실행

```powershell
.\빠른_경로_확인.bat
```

이 파일이 자동으로 경로를 확인하고 올바른 경로를 알려줍니다.

---

### 방법 2: 파일 탐색기에서 확인

1. **파일 탐색기 열기**
2. 다음 경로로 이동:
   ```
   C:\oraclexe\instantclient-basic-windows.x64-23.26.0.0.0
   ```

3. **`oci.dll` 파일 찾기**
   - 이 폴더에 바로 `oci.dll`이 보이나요?
   - 또는 하위 폴더에 있나요?

---

## 📝 올바른 경로 판단

### 경우 1: `oci.dll`이 바로 있는 경우

```
C:\oraclexe\instantclient-basic-windows.x64-23.26.0.0.0\
  └── oci.dll  ← 여기!
```

**사용할 경로**:
```
C:\oraclexe\instantclient-basic-windows.x64-23.26.0.0.0
```

### 경우 2: 하위 폴더에 있는 경우

```
C:\oraclexe\instantclient-basic-windows.x64-23.26.0.0.0\
  └── instantclient_23_26\
      └── oci.dll  ← 여기!
```

**사용할 경로**:
```
C:\oraclexe\instantclient-basic-windows.x64-23.26.0.0.0\instantclient_23_26
```

---

## 🔧 .env 파일 설정

올바른 경로를 찾았으면 `.env` 파일에 추가:

```env
ORACLE_INSTANT_CLIENT_PATH=C:\oraclexe\instantclient-basic-windows.x64-23.26.0.0.0
```

또는 하위 폴더에 있다면:

```env
ORACLE_INSTANT_CLIENT_PATH=C:\oraclexe\instantclient-basic-windows.x64-23.26.0.0.0\instantclient_23_26
```

---

## ✅ 테스트

경로를 설정한 후:

```powershell
python test_oracle_thick.py
```

성공하면:
```
✅ Thick 모드 활성화 성공!
✅ Thick 모드로 연결 성공!
```

---

## 💡 팁

**경로 이름이 너무 길다면** 폴더 이름을 변경하세요:
- `C:\oraclexe\instantclient_23_26` (더 짧게)

또는:
- `C:\oracle\instantclient_23_26` (표준 위치)



