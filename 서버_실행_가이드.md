# Django 서버 실행 가이드

## ❌ 오류 원인

```
ModuleNotFoundError: No module named 'django'
ImportError: Couldn't import Django. Are you sure it's installed and available on your PYTHONPATH environment variable? Did you forget to activate a virtual environment?
```

**원인**: 가상환경(Virtual Environment)이 활성화되지 않아서 Django를 찾을 수 없습니다.

---

## ✅ 해결 방법

### 방법 1: PowerShell에서 실행 (권장)

```powershell
# 1. 프로젝트 디렉토리로 이동
cd "C:\Users\134\Desktop\DX Project"

# 2. 가상환경 활성화
.\venv\Scripts\Activate.ps1

# 3. 서버 실행
python manage.py runserver
```

### 방법 2: Command Prompt (CMD)에서 실행

```cmd
# 1. 프로젝트 디렉토리로 이동
cd C:\Users\134\Desktop\DX Project

# 2. 가상환경 활성화
venv\Scripts\activate.bat

# 3. 서버 실행
python manage.py runserver
```

### 방법 3: 배치 파일로 한 번에 실행

프로젝트 루트에 `run_server.bat` 파일을 만들고:

```batch
@echo off
cd /d "%~dp0"
call venv\Scripts\activate.bat
python manage.py runserver
pause
```

더블클릭하면 자동으로 실행됩니다.

---

## 🔍 확인 방법

### 가상환경이 활성화되었는지 확인

가상환경이 활성화되면 프롬프트 앞에 `(venv)`가 표시됩니다:

```
(venv) PS C:\Users\134\Desktop\DX Project>
```

### Django 설치 확인

```powershell
python -m pip list | Select-String django
```

출력 예시:
```
Django              5.2.8
djangorestframework 3.16.1
```

---

## 🚨 PowerShell 실행 정책 오류가 발생하는 경우

만약 PowerShell에서 다음과 같은 오류가 발생하면:

```
.venv\Scripts\Activate.ps1: 이 시스템에서 스크립트를 실행할 수 없으므로 ...
```

**해결 방법:**

1. PowerShell을 **관리자 권한**으로 실행
2. 실행 정책 변경:
   ```powershell
   Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```
3. 다시 가상환경 활성화 시도

---

## 📝 빠른 참조

| 작업 | 명령어 |
|------|--------|
| 가상환경 활성화 (PowerShell) | `.\venv\Scripts\Activate.ps1` |
| 가상환경 활성화 (CMD) | `venv\Scripts\activate.bat` |
| 가상환경 비활성화 | `deactivate` |
| 서버 실행 | `python manage.py runserver` |
| 서버 실행 (다른 포트) | `python manage.py runserver 8080` |
| Django 설치 확인 | `python -m pip list \| Select-String django` |

---

## ✅ 정상 실행 확인

서버가 정상적으로 실행되면 다음과 같은 메시지가 표시됩니다:

```
Watching for file changes with StatReloader
Performing system checks...

System check identified no issues (0 silenced).
December 02, 2024 - XX:XX:XX
Django version 5.2.8, using settings 'config.settings'
Starting development server at http://127.0.0.1:8000/
Quit the server with CTRL-BREAK.
```

이제 브라우저에서 `http://127.0.0.1:8000/` 또는 `http://localhost:8000/`로 접속할 수 있습니다!


