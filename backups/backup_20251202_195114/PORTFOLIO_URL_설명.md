# 포트폴리오 URL이란?

## 📋 개요

`http://127.0.0.1:8000/portfolio/PF-QU81AI/` 같은 URL은 **저장된 추천 결과를 공유하기 위한 고유한 링크**입니다.

## 🎯 기능

### 1. 추천 결과 저장 및 공유
- 사용자가 온보딩을 완료하고 추천 결과를 받은 후
- "저장" 버튼을 클릭하면 데이터베이스에 저장됨
- 고유한 `portfolio_id`가 생성됨 (예: `PF-QU81AI`)
- 이 ID를 통해 언제든지 결과를 다시 볼 수 있음

### 2. 공유 기능
- 카카오톡으로 공유할 때 이 링크를 사용
- 링크를 받은 사람도 같은 추천 결과를 볼 수 있음
- 나중에 다시 보고 싶을 때 이 링크로 접근 가능

## 🔧 동작 방식

1. **저장**: `/api/portfolio/save/` API를 통해 추천 결과 저장
2. **생성**: 고유한 `portfolio_id` 생성 (예: `PF-QU81AI`)
3. **표시**: `/portfolio/PF-QU81AI/` URL로 접근하면 저장된 결과 표시
4. **공유**: 이 URL을 카카오톡 등으로 공유

## 📝 현재 상태

- ✅ 포트폴리오 저장 기능 구현됨
- ✅ portfolio_id 생성 기능 구현됨
- ✅ URL 패턴 설정됨 (`/portfolio/<portfolio_id>/`)
- ⚠️ URL에서 포트폴리오 데이터 자동 로드 기능 개선 필요

## 🔍 코드 위치

- **모델**: `api/models.py` - `Portfolio` 클래스
- **뷰**: `api/views.py` - `portfolio_save_view`, `portfolio_detail_view`
- **URL**: `config/urls.py` - `path('portfolio/<str:portfolio_id>/', ...)`
- **템플릿**: `api/templates/result.html` - 포트폴리오 로드 로직

